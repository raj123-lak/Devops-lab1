pipeline {
    agent any

    triggers {
        pollSCM('* * * * *')   
    }

    stages {
        stage('Run') {
            steps {
                echo "Multibranch pipeline triggered!"
            }
        }
    }
}
